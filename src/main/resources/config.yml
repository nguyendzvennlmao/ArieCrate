sounds:
  gui-click: UI_BUTTON_CLICK
  no-key: ENTITY_ILLAGER_CAST_SPELL
  reward: ENTITY_PLAYER_LEVELUP

settings:
  chat:
    enabled: true
  actionbar:
    enabled: true

keyall:
  enabled: true
  subtitle:
    enabled: true
    format: "&f+{amount} &e{key_name}"
  command: "acrate givekey {player} common 1"
  time: 1h
